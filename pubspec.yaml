name: villaguest
description: ""
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # Firebase
  firebase_core: ^3.8.0
  firebase_auth: ^5.3.3
  cloud_firestore: ^5.5.0
  firebase_storage: ^12.3.7
  
  # Estado
  provider: ^6.1.2

  # Utilidades
  intl: ^0.20.3                  # Formateo de fechas y monedas
  image_picker: ^1.1.2           # Para cámara/fotos (limpieza)
  url_launcher: ^6.2.1           # Abrir enlaces externos

  # Generación y descarga de PDFs (facturas)
  pdf: ^3.11.1
  printing: ^5.13.2
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0


flutter:
  uses-material-design: true
